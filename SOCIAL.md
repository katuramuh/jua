# Jua + Jua Cloud — 30-Day Social Media Content Plan

**Products:** Jua (open-source framework) + Jua Cloud (managed deployment platform)
**Creator:** katuramuh | GitHub: github.com/katuramuh | Website: juaframework.dev
**Tagline:** Go + React. Built with Jua.

---

## Day 1

### LinkedIn Post

Something I've been building in silence for months is finally ready to see the light.

Introducing **Jua** — a full-stack meta-framework that pairs the raw performance of Go (Gin + GORM) with the developer experience of Next.js 14 and React.

If you've ever loved what Laravel does for PHP, or what Rails did for Ruby, but always wished you could get that same productivity while keeping Go's speed and concurrency — Jua is what I built for you.

Here's what you get out of the box:

- CLI scaffolder: `jua new myapp` and you're running in seconds
- Code generator: `jua generate resource Post` creates your model, routes, handlers, and frontend pages — all wired together
- A Filament-inspired admin panel with zero configuration
- Background jobs via asynq + Redis
- File storage (S3, R2, MinIO) ready to go
- Email via Resend, AI integration (Claude + OpenAI), Redis caching
- GORM Studio — a visual database browser baked in

This is open source. This is for builders. This is for developers who refuse to choose between speed and productivity.

Star it on GitHub. Try it today. Tell me what you think.

juaframework.dev

#GoLang #React #OpenSource #WebDev #Jua #FullStack #DeveloperTools

---

### Tweet/X

Introducing Jua — Go + React, full-stack, open source.

`jua new myapp` → you get a backend (Gin + GORM), a Next.js 14 frontend, admin panel, background jobs, file storage, and email. All wired.

Like Laravel. But Go. 🔥

juaframework.dev #GoLang #OpenSource #Jua

---

## Day 2

### LinkedIn Post

What problem does Jua actually solve?

I've worked with Go backends and React frontends for years. The productivity gap is real. Setting up a Go REST API, wiring it to a Next.js frontend, configuring CORS, building an admin panel, adding background jobs, integrating file storage — it's weeks of boilerplate before you write a single line of business logic.

Jua closes that gap.

One command: `jua new myapp`

You get:
- A Go backend powered by Gin and GORM
- A Next.js 14 + React frontend
- Authentication scaffolded and ready
- An admin panel (think Filament for Go)
- Redis-backed background job queue
- Database browser via GORM Studio
- File storage connectors (S3, R2, MinIO)
- Email (Resend) and AI (Claude, OpenAI) integrations

This isn't just a starter template. It's an opinionated, production-ready framework with conventions that let you move fast without sacrificing Go's legendary performance.

The boilerplate era is over.

juaframework.dev — Go try it.

#GoLang #Gin #GORM #NextJS #React #OpenSource #Jua

---

### Tweet/X

The Go ecosystem has amazing libraries. What it's been missing is a framework that wires them together with real conventions.

That's Jua.

`jua new myapp` and stop writing boilerplate forever. #GoLang #Jua

---

Tweet 2:

Go is fast. React is loved. Jua puts them together.

One command. Full stack. Open source.

juaframework.dev #GoLang #React #Jua #WebDev

---

## Day 3

### LinkedIn Post

Open source isn't just a license. It's a philosophy.

When I started building Jua, I made a decision early: this would be open source, always. Not open-core. Not "free tier with paywalled features." Fully open.

Here's why that matters for a framework:

Frameworks live and die by trust. Developers need to know that the foundation of their applications won't disappear behind a pricing page. They need to read the source, contribute fixes, understand exactly what's happening under the hood.

Jua is MIT-licensed. Every feature — the CLI, the code generator, the admin panel, the job queue, the storage connectors, the AI integrations — is in the open.

github.com/katuramuh/jua

If you've been waiting for a Go framework with real full-stack conventions, the wait is over. Clone it, try it, and if something doesn't work the way you expect — open a PR. That's the deal.

Go + React. Built with Jua.

#GoLang #OpenSource #MIT #React #FullStack #Jua #DeveloperCommunity

---

### Tweet/X

Jua is MIT-licensed. All of it. No open-core tricks, no "enterprise edition" paywalls.

The CLI, admin panel, job queues, AI integrations — all open source.

github.com/katuramuh/jua #GoLang #OpenSource #Jua

---

## Day 4

### LinkedIn Post

Let me show you what "full-stack in one command" actually means.

You run:

```
jua new myapp
cd myapp
jua dev
```

And you have:

**Backend (Go)**
- Gin HTTP router with structured routes
- GORM with PostgreSQL, MySQL, or SQLite support
- Authentication middleware (JWT)
- Redis connection (caching + job queue)
- File storage service abstracted behind a single interface

**Frontend (Next.js 14 + React)**
- App Router with server and client components
- API client auto-configured to talk to your Go backend
- Tailwind CSS with a component system
- Pre-built auth pages (login, register, forgot password)

**Admin Panel**
- CRUD interfaces generated automatically
- Resource-based navigation
- User management baked in

**Everything else**
- Background job workers running alongside your server
- Database migrations managed by GORM
- Environment config with sane defaults

This is the full-stack Go experience developers deserve.

juaframework.dev

#GoLang #NextJS #React #Gin #GORM #FullStack #Jua

---

### Tweet/X

`jua new myapp && jua dev`

That's it. Go backend (Gin + GORM) + Next.js 14 frontend + admin panel + Redis jobs + file storage — running locally, ready for production.

No config. No boilerplate. Just build. #GoLang #Jua

---

## Day 5

### LinkedIn Post

Why "Jua"?

It's a word that means persistence. Determination. The willingness to keep going when the easy path would be to stop.

Go has jua. It's not the flashiest language. It doesn't have the trendiest features. But it's been quietly powering some of the most critical infrastructure on the planet for years — Kubernetes, Docker, Cloudflare's edge network, Uber's microservices.

Go developers have jua. They choose correctness over cleverness. Performance over abstraction for its own sake. Readable code over magic.

This framework is named for that quality.

**Jua** is for developers who want to build real things that last. Who are tired of choosing between the productivity of a modern framework and the performance of a systems language. Who believe Go deserves a full-stack story as good as what Laravel gave PHP or Rails gave Ruby.

That's the mission.

Go + React. Built with Jua.

Star the repo: github.com/katuramuh/jua
Try the framework: juaframework.dev

#GoLang #OpenSource #WebDev #Jua #BackendDevelopment #React #DeveloperTools

---

### Tweet/X

Go has been powering Kubernetes, Docker, and Cloudflare for years.

It deserves a full-stack framework as good as what Laravel gave PHP.

That's Jua. juaframework.dev #GoLang #Jua #OpenSource

---

Tweet 2:

Built with Go's reliability. Shipped with React's UX. Scaffolded in one command.

"Jua" — because building great software takes exactly that.

github.com/katuramuh/jua #GoLang #Jua

---

## Day 6

### LinkedIn Post

Let's talk about the Jua CLI — because the developer experience starts here.

The CLI is the front door of the entire framework. When you install Jua, the first thing you run is:

```
jua new myapp
```

It prompts you for your database (PostgreSQL, MySQL, SQLite), your preferred file storage backend (local, S3, R2, MinIO), and a few other options. Then it scaffolds a complete, working application in seconds.

But the CLI doesn't stop at project creation. It follows you through the entire development lifecycle:

```bash
jua dev              # Start the dev server (backend + frontend concurrently)
jua generate resource Post  # Generate a full resource (model + routes + UI)
jua db migrate       # Run pending database migrations
jua db studio        # Open GORM Studio — your visual database browser
jua deploy           # Deploy to Jua Cloud
```

Every command is designed around one principle: reduce friction. The commands you'd type dozens of times a day should feel effortless.

This is what full-stack Go development should feel like.

juaframework.dev

#GoLang #CLI #DeveloperExperience #DevTools #OpenSource #Jua #FullStack

---

### Tweet/X

The Jua CLI:

```
jua new myapp       # scaffold full-stack Go + React app
jua dev             # run everything concurrently
jua db studio       # open visual DB browser
jua deploy          # ship to production
```

Four commands. Entire lifecycle covered. #GoLang #Jua #DeveloperExperience

---

## Day 7

### LinkedIn Post

The code generator in Jua might be my favorite feature to demo.

Watch what happens when you run this single command:

```bash
jua generate resource Post --fields "title:string,content:text,published:bool"
```

Jua generates:

**Backend:**
- A GORM model with your fields, timestamps, and soft delete
- Migration file ready to run
- Gin route group (`GET /posts`, `POST /posts`, `GET /posts/:id`, `PUT /posts/:id`, `DELETE /posts/:id`)
- Request handler with validation
- Service layer with business logic separation

**Frontend:**
- Next.js page for listing posts (with pagination)
- Detail page for a single post
- Create/edit form with Zod validation
- API client functions auto-typed against your Go response shapes

**Admin Panel:**
- Resource registered in the admin sidebar
- CRUD table with sortable columns
- Create/edit forms with field types inferred from your schema

All from one command. All wired together. All production-quality.

This is the promise of Jua: you spend your time on what makes your application unique, not on the infrastructure that looks the same in every app you ever build.

juaframework.dev

#GoLang #CodeGeneration #DeveloperProductivity #React #GORM #Jua #OpenSource

---

### Tweet/X

```
jua generate resource Post \
  --fields "title:string,content:text,published:bool"
```

→ GORM model + migration
→ Gin routes + handlers
→ Next.js pages + forms
→ Admin panel CRUD
→ Typed API client

One command. Full stack. #GoLang #Jua

---

## Day 8

### LinkedIn Post

Let's talk about the Jua Admin Panel — and why I built it.

Every application needs some kind of admin interface. Content management, user management, reviewing orders, moderating submissions. The admin panel is always needed and almost never the part you want to spend time building.

In the PHP ecosystem, Filament solved this beautifully. In the Go ecosystem, there was... nothing with the same ergonomics.

Jua's admin panel is inspired by Filament. You define resources in your Go code, and the panel builds the UI for you:

- Auto-generated tables with sortable, filterable columns
- Create and edit forms with field type awareness (text, number, boolean, select, file upload)
- Relation management (has-many, belongs-to)
- Action buttons with confirmation dialogs
- Role-based access control

When you run `jua generate resource`, your resource is automatically registered in the admin panel. You don't write a single line of UI code for admin functionality unless you want to customize it.

The admin panel is built on React and served from your Next.js frontend — so you get the full Next.js ecosystem for customizations.

No external admin service. No separate deployment. One codebase.

juaframework.dev

#GoLang #AdminPanel #React #NextJS #Filament #FullStack #Jua

---

### Tweet/X

Jua has a Filament-inspired admin panel baked in.

Define your resources in Go → get a full CRUD admin UI automatically: tables, forms, filters, relations, RBAC.

No separate deployment. No extra config. Just `jua generate resource`. #GoLang #Jua #AdminPanel

---

## Day 9

### LinkedIn Post

AI-ready from day one.

I'm tired of AI being a bolt-on afterthought. When you build a new application today, there's a good chance AI is part of the product — whether it's content generation, intelligent search, chat interfaces, or data summarization.

Jua ships with first-class AI integrations for both Claude and OpenAI.

Out of the box you get:

```go
// In your Go handler
response, err := ai.Generate(ctx, ai.GenerateOptions{
    Provider: "claude",  // or "openai"
    Model:    "claude-opus-4-6",
    Prompt:   "Summarize this article: " + article.Content,
})
```

The AI service abstraction means you can switch providers without changing your application code. Both streaming and non-streaming responses are supported.

On the frontend side, Jua's React components include a `<AIChat>` component and streaming response hooks that wire directly to your Go backend.

Set your API keys in `.env`. Start building AI features.

No SDK setup. No proxy configuration. No boilerplate.

juaframework.dev

#GoLang #AI #Claude #OpenAI #React #FullStack #Jua

---

### Tweet/X

Jua ships with Claude + OpenAI integrations out of the box.

```go
ai.Generate(ctx, ai.GenerateOptions{
    Provider: "claude",
    Model:    "claude-opus-4-6",
    Prompt:   "Summarize: " + content,
})
```

AI-ready from day one. No setup. #GoLang #AI #Jua #Claude

---

## Day 10

### LinkedIn Post

Background jobs are the unsung hero of every serious web application.

Sending emails asynchronously. Processing image uploads. Generating reports. Syncing data with third-party APIs. Scheduling recurring tasks. All of these need to happen outside the HTTP request cycle — and doing them right is harder than it looks.

Jua solves this with asynq + Redis, and integrates it so seamlessly you might not even notice it's there until you need it.

```go
// Enqueue a job from anywhere in your Go code
err := jobs.Enqueue(ctx, &EmailJob{
    To:      "user@example.com",
    Subject: "Welcome to the app",
    Template: "welcome",
})
```

Your job handler:

```go
func (j *EmailJob) Process(ctx context.Context) error {
    return email.Send(ctx, j.To, j.Subject, j.Template)
}
```

That's it. The job is serialized, stored in Redis, and processed by a worker pool running alongside your API server. Retries, dead-letter queues, job scheduling — all handled by asynq.

You can monitor your queues directly in the Jua admin panel. No separate monitoring setup.

Production-grade background jobs in minutes, not days.

juaframework.dev

#GoLang #BackgroundJobs #Redis #Asynq #FullStack #WebDev #Jua

---

### Tweet/X

Background jobs in Jua:

```go
jobs.Enqueue(ctx, &EmailJob{
    To: "user@example.com",
    Template: "welcome",
})
```

Powered by asynq + Redis. Retries, scheduling, dead-letter queues, queue monitoring in admin panel.

Production-grade. Zero setup. #GoLang #Jua #Redis

---

## Day 11

### LinkedIn Post

"Why not just use Laravel?"

It's a fair question. Laravel is incredible. It's one of the best frameworks ever built, and PHP's ecosystem is genuinely world-class.

Here's the honest answer: if your team is deep in PHP, use Laravel. It's excellent.

But if you want or need Go — for performance, for concurrency, for your infrastructure's language, for your team's preferences — you've been stuck choosing between Laravel-level productivity and Go's performance. Until now.

Jua brings the Laravel experience to Go:

| Laravel | Jua (Go) |
|---|---|
| `php artisan make:model Post` | `jua generate resource Post` |
| Eloquent ORM | GORM |
| Filament admin | Jua Admin Panel |
| Queues + Horizon | asynq + Redis |
| Livewire/Inertia | Next.js 14 + React |
| Laravel Forge | Jua Cloud |

Same concepts. Same productivity. Go's performance.

Go handles concurrent requests with goroutines using a fraction of the memory that PHP needs. Your Jua app can handle 10x the traffic on the same hardware.

You don't have to choose between productivity and performance anymore.

juaframework.dev

#GoLang #Laravel #PHP #WebDev #FullStack #Jua #Performance

---

### Tweet/X

Laravel devs: Jua is your Go answer.

`jua generate resource Post` = artisan make:model + controller + views
GORM = Eloquent
Jua Admin = Filament
asynq = Horizon
Jua Cloud = Forge

Same productivity. Go's performance. #GoLang #Laravel #Jua

---

## Day 12

### LinkedIn Post

"Why not just use Next.js for everything?"

Next.js 14 with API routes is genuinely powerful. You can build a full-stack app with it. I have. Many developers have.

Here's where it starts to hurt:

**Concurrency.** Node.js is single-threaded. When you have CPU-intensive tasks — image processing, PDF generation, data transformations — you're fighting the event loop.

**Background jobs.** You end up adding BullMQ or similar, which means more infrastructure, more complexity, more things to monitor.

**Type safety across the stack.** You can do it in TypeScript, but it requires discipline and the right tooling to keep in sync.

**Performance at scale.** Node can handle a lot, but Go handles more with less memory. This matters when your bill arrives.

Jua's approach: use the right tool for each layer. Go handles the heavy lifting on the backend — HTTP routing (Gin), database (GORM), background jobs (asynq), caching (Redis). Next.js 14 handles what it's actually great at: building incredible user interfaces with React Server Components.

You get Go's concurrency for your API and Next.js's component model for your UI.

Best of both worlds. One framework.

juaframework.dev

#GoLang #NextJS #React #NodeJS #WebDev #FullStack #Jua

---

### Tweet/X

Next.js is great for UI. Go is great for backend concurrency + performance.

Jua doesn't make you choose.

Go (Gin + GORM) handles the API layer. Next.js 14 handles the UI layer. Both generated and wired by one CLI.

#GoLang #NextJS #Jua #FullStack

---

## Day 13

### LinkedIn Post

"Why not just use Ruby on Rails?"

Rails shaped how we think about web frameworks. Convention over configuration, generators, the MVC pattern, Active Record — these ideas revolutionized web development. I have enormous respect for Rails.

But Rails in 2026 has real tradeoffs:

- Ruby is slow. Not unusably slow, but at scale, you pay for it in hosting costs.
- Concurrency is painful. Puma + threads is fine for many apps, but goroutines handle concurrent work at a different level.
- The JavaScript bridge is always awkward. Hotwire, Stimulus, Turbo — they're clever, but you're always fighting the seam between server-rendered HTML and modern UI expectations.

Jua takes the Rails philosophy — generators, conventions, "it just works" — and applies it to the Go + React world.

`jua generate resource Post` is your `rails generate scaffold Post`. It generates everything — model, routes, handlers, UI pages, admin panel entry — and it's all Go + React, not Ruby + ERB.

You get the productivity that made Rails legendary, with Go's performance and React's UI power.

The next generation of opinionated full-stack frameworks is here.

juaframework.dev

#GoLang #RubyOnRails #Ruby #React #FullStack #Jua #WebDev

---

### Tweet/X

Rails gave us "convention over configuration" and changed web dev forever.

Jua brings that philosophy to Go + React.

`jua generate resource Post` = `rails g scaffold Post` but with Go's performance and React's UI.

juaframework.dev #GoLang #Rails #Jua

---

## Day 14

### LinkedIn Post

Let me address the elephant in the room: Go is verbose.

I know. `if err != nil` everywhere. Explicit types. No exceptions. No magic.

Here's my take: the verbosity is a feature, not a bug — in the right places. Where Go is explicit, it's preventing a whole class of runtime surprises that haunt dynamically-typed backend code.

But boilerplate? Boilerplate is different. Boilerplate is writing the same struct, the same handler pattern, the same CRUD routes, the same pagination logic in every project you ever build. That's not explicitness — that's just tedious repetition.

That's exactly what Jua's generator eliminates.

```bash
jua generate resource Post \
  --fields "title:string,content:text,status:string"
```

The generated Go code is explicit, idiomatic, and readable. No magic. No reflection tricks. Real Go code you can read and modify.

You get Go's clarity without Go's boilerplate tax.

That's the promise: keep everything that makes Go good (performance, concurrency, type safety, readability), eliminate everything that slows you down at the start of a project.

juaframework.dev

#GoLang #WebDev #DeveloperProductivity #Jua #OpenSource #FullStack #BackendDevelopment

---

### Tweet/X

Go is verbose by design. Explicit error handling, strong types — that's good.

But writing the same CRUD boilerplate in every project? That's just tedious.

Jua generates the boilerplate. You write the business logic.

#GoLang #Jua #DeveloperProductivity

---

## Day 15

### LinkedIn Post

Go vs the world — let's talk performance numbers.

This isn't a "my framework is better" post. It's a "here's what you're getting when you choose Go" post.

Some real-world benchmarks from the Go ecosystem:

- **Gin** (the HTTP router Jua uses) handles ~60,000 requests/second on modest hardware. Express.js handles roughly 15,000 on the same hardware.
- **Memory:** A Go HTTP server typically uses 10-30MB at idle. A Node.js server doing the same work often uses 100-200MB.
- **Goroutines:** Go can handle hundreds of thousands of concurrent goroutines. Threads in other runtimes are far more expensive.
- **Cold start:** Go binaries start in milliseconds. JVM-based apps often take seconds.

For small projects, these differences don't matter. You can scale Node or PHP to impressive heights with the right infrastructure.

But when your app grows, Go compounds in your favor. Lower memory = more instances per machine = lower hosting costs. Better concurrency = fewer servers needed.

Jua gives you all of this — packaged with the productivity of a full-stack framework.

High performance doesn't have to be hard to build with.

juaframework.dev

#GoLang #Performance #WebDev #Gin #FullStack #Jua #BackendDevelopment

---

### Tweet/X

Gin (Go) handles ~60K req/s.
Express (Node) handles ~15K req/s.
Go idle memory: ~20MB.
Node idle memory: ~150MB.

Jua is built on Gin. You get this performance + full-stack conventions.

No trade-offs. #GoLang #Performance #Jua

---

## Day 16

### LinkedIn Post

Tutorial time. Let's build a blog API in Jua from zero.

**Step 1: Create your project**
```bash
jua new myblog
cd myblog
```

**Step 2: Generate the Post resource**
```bash
jua generate resource Post \
  --fields "title:string,slug:string,content:text,published:bool,author_id:uint"
```

**Step 3: Run migrations**
```bash
jua db migrate
```

**Step 4: Start the dev server**
```bash
jua dev
```

You now have:
- `GET /api/posts` — paginated list of posts
- `POST /api/posts` — create a post (with validation)
- `GET /api/posts/:id` — get a single post
- `PUT /api/posts/:id` — update a post
- `DELETE /api/posts/:id` — soft delete

Plus a Next.js frontend at `localhost:3000` with pages for listing, creating, and editing posts.

Plus the admin panel at `localhost:3000/admin` with full CRUD for posts.

Total time: under 3 minutes.

That's Jua. That's the power of a full-stack Go framework done right.

juaframework.dev

#GoLang #Tutorial #GORM #Gin #React #NextJS #Jua

---

### Tweet/X

Build a full blog API + frontend + admin panel in Jua:

```bash
jua new myblog
jua generate resource Post \
  --fields "title:string,content:text,published:bool"
jua db migrate
jua dev
```

Done. 3 minutes. Go + React + Admin. #GoLang #Jua #Tutorial

---

## Day 17

### LinkedIn Post

Let's look at what Jua generates when you create a resource — the actual Go code.

After running `jua generate resource Post`, your handler looks like this:

```go
// internal/handlers/post_handler.go

func (h *PostHandler) List(c *gin.Context) {
    page := utils.GetPage(c)
    posts, total, err := h.postService.List(c.Request.Context(), page)
    if err != nil {
        c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
        return
    }
    c.JSON(http.StatusOK, gin.H{
        "data":  posts,
        "total": total,
        "page":  page,
    })
}

func (h *PostHandler) Create(c *gin.Context) {
    var req CreatePostRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})
        return
    }
    post, err := h.postService.Create(c.Request.Context(), req)
    if err != nil {
        c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
        return
    }
    c.JSON(http.StatusCreated, post)
}
```

Real Go code. Idiomatic. Readable. No magic. No reflection tricks. Easy to modify.

This is the Jua philosophy: generate the boring parts so you can focus on what makes your application unique.

juaframework.dev

#GoLang #Gin #CodeGeneration #Tutorial #WebDev #Jua #OpenSource

---

### Tweet/X

Jua generates real, idiomatic Go code — not templates you're afraid to touch.

After `jua generate resource Post`, you get clean Gin handlers with proper error handling, pagination, and validation.

Read it. Own it. Modify it. #GoLang #Gin #Jua

---

## Day 18

### LinkedIn Post

File storage in Jua — upload once, store anywhere.

One of the annoying parts of any web application is file uploads. You start with local storage during development, then need to switch to S3 (or R2, or MinIO) for production. The code changes. The config changes. It's a headache.

Jua abstracts this behind a single `storage` interface. Change your backend without changing your application code.

```go
// This works with local, S3, R2, or MinIO
// — just change the config
file, err := storage.Upload(ctx, storage.UploadOptions{
    File:        uploadedFile,
    Path:        "posts/" + postID,
    ContentType: "image/jpeg",
})

// Get the public URL
url := storage.URL(file.Key)

// Delete the file
err = storage.Delete(ctx, file.Key)
```

Configure your backend in `.env`:

```
STORAGE_DRIVER=s3          # local | s3 | r2 | minio
AWS_BUCKET=my-bucket
AWS_REGION=us-east-1
```

Switch from local to S3? Change one environment variable. No code changes.

When you deploy to Jua Cloud, MinIO is provisioned for you automatically. Zero configuration.

File storage that just works.

juaframework.dev

#GoLang #FileStorage #S3 #CloudStorage #WebDev #Jua #FullStack

---

### Tweet/X

File storage in Jua:

```go
file, _ := storage.Upload(ctx, storage.UploadOptions{
    File: uploadedFile,
    Path: "posts/" + id,
})
url := storage.URL(file.Key)
```

Works with local, S3, R2, or MinIO. Change `STORAGE_DRIVER` in `.env`. Same code everywhere. #GoLang #Jua #S3

---

## Day 19

### LinkedIn Post

Redis caching in Jua — make your app fast by default.

Caching is one of those things that every developer knows they should be doing and few actually implement well. It's not hard, but it requires discipline.

Jua makes it embarrassingly easy.

```go
// Cache the result of an expensive query
posts, err := cache.Remember(ctx, "posts:featured", 15*time.Minute, func() ([]Post, error) {
    return h.postService.GetFeatured(ctx)
})

// Invalidate cache when data changes
cache.Forget(ctx, "posts:featured")

// Cache arbitrary values
cache.Set(ctx, "user:"+userID+":profile", profile, 1*time.Hour)
profile, err := cache.Get[Profile](ctx, "user:"+userID+":profile")
```

The cache service is backed by Redis (the same Redis instance used for background jobs). In development, it falls back gracefully if Redis isn't running.

On Jua Cloud, Redis is provisioned automatically alongside your application. You don't configure a connection string — it's wired in at deploy time.

Your application should be fast on day one. Jua makes that the default, not an afterthought.

juaframework.dev

#GoLang #Redis #Caching #Performance #WebDev #Jua #FullStack

---

### Tweet/X

Redis caching in Jua:

```go
posts, err := cache.Remember(ctx,
    "posts:featured",
    15*time.Minute,
    func() ([]Post, error) {
        return service.GetFeatured(ctx)
    })
```

Backed by Redis. Auto-provisioned on Jua Cloud. Performance by default. #GoLang #Redis #Jua

---

## Day 20

### LinkedIn Post

Let's talk about GORM Studio — the visual database browser built into Jua.

I know "database browser" doesn't sound exciting. Stick with me.

When you're building an application, you spend a LOT of time looking at data. What's actually in the database? Is my migration correct? What does this relationship look like? Why is this query returning unexpected results?

Most developers have a separate tool for this — TablePlus, DBeaver, pgAdmin. You switch out of your terminal, open another app, reconnect to your database.

Jua has GORM Studio built in.

```bash
jua db studio
```

Opens a web UI at `localhost:8080/studio` where you can:

- Browse all your tables with full data
- Run raw SQL queries
- Inspect GORM model relationships
- View migration history
- Execute seeds

It's not a replacement for a dedicated database tool for complex analysis. But for the day-to-day "let me just check what's in this table" workflow, it's right there — no context switching.

One less tool to install. One less thing to configure.

juaframework.dev

#GoLang #GORM #Database #DeveloperTools #WebDev #Jua #PostgreSQL

---

### Tweet/X

`jua db studio`

Opens a visual database browser in your browser. Browse tables, run queries, inspect relationships, view migration history.

No separate app to install. No reconnecting. Right there in your dev setup. #GoLang #GORM #Jua #DevTools

---

## Day 21

### LinkedIn Post

Introducing **Jua Cloud** — deploy your Jua app in 60 seconds.

You've built your app with Jua. It's ready. Now what?

Deployment is where a lot of frameworks fall short. You're on your own — configure a VPS, set up Nginx, manage your database, provision Redis, handle SSL certificates, configure your object storage, wire up your environment variables.

Jua Cloud is the answer.

```bash
jua deploy
```

That's it. One command.

Jua Cloud knows your app inside out — because it's purpose-built for Jua apps. When you deploy, you automatically get:

- **PostgreSQL** — managed, backed up daily, with connection pooling
- **Redis** — for your caching and background job queues
- **MinIO file storage** — S3-compatible object storage, provisioned and connected
- **Email infrastructure** — Resend integration ready to go
- **SSL/TLS** — automatic HTTPS for your domain
- **Auto-scaling** — your app scales with traffic, scales to zero when idle

Pay as you go. No surprise bills. No idle server costs.

Like Railway or Render, but built specifically for Jua apps. Every feature Jua uses has a corresponding managed service in Jua Cloud.

juaframework.dev/cloud

#GoLang #CloudDeployment #DevOps #Jua #JuaCloud #FullStack #OpenSource

---

### Tweet/X

Jua Cloud is live.

`jua deploy`

→ PostgreSQL provisioned
→ Redis provisioned
→ MinIO file storage provisioned
→ Email infrastructure ready
→ SSL + auto-scaling included

One command. 60 seconds. Production-ready. juaframework.dev/cloud #JuaCloud #GoLang

---

## Day 22

### LinkedIn Post

Why does "purpose-built for Jua" matter?

General-purpose platforms like Railway, Render, and Fly.io are excellent. I've used all of them. But "general-purpose" means you still need to configure things yourself:

- "How do I connect my app container to my database container?"
- "How do I pass my Redis URL to my background worker?"
- "How do I wire my MinIO bucket credentials to my storage service?"
- "Do I need separate deployments for my frontend and backend?"

When your platform doesn't know your framework, you answer these questions yourself. Every time. For every project.

Jua Cloud knows Jua intimately.

It knows that your `STORAGE_DRIVER=minio` needs a MinIO instance. It provisions one and injects the credentials.

It knows that your `REDIS_URL` is used for both caching and asynq job queues. It provisions Redis and connects both.

It knows your frontend is Next.js and your backend is a Go binary — and it serves them correctly as a unified application.

Zero configuration. Zero guesswork. Deploy and go.

juaframework.dev/cloud

#GoLang #CloudPlatform #DevOps #JuaCloud #DeveloperExperience #FullStack #Jua

---

### Tweet/X

General cloud platforms: "how do I connect Redis to my job queue?"

Jua Cloud: already done. It knows you're running asynq. Redis is wired in automatically.

Purpose-built for Jua apps. No guesswork. #JuaCloud #GoLang #DevOps

---

## Day 23

### LinkedIn Post

The economics of Jua Cloud — let's talk money.

Hosting costs matter. Especially when you're starting out, shipping side projects, or running an early-stage startup.

Jua Cloud is pay-as-you-go, with scale-to-zero for apps that aren't receiving traffic.

What does that mean in practice?

A side project that gets occasional traffic might cost you **$0/month** most of the time — it scales to zero when idle and spins up in milliseconds when a request arrives.

An active production app with steady traffic gets:
- Managed PostgreSQL (no devops work, automated backups)
- Managed Redis (no configuration, just use it)
- Managed MinIO (S3-compatible storage that scales)
- Automatic SSL, automatic scaling, automatic failover

Compare this to the alternative: a VPS on DigitalOcean/Hetzner where you're managing updates, SSL certificates, database backups, Redis configuration, and monitoring yourself.

Jua Cloud trades the DevOps overhead for a usage-based price.

For most early-stage applications, Jua Cloud is cheaper in combined dollars + developer time than self-hosting.

Start free. Scale as you grow.

juaframework.dev/cloud

#GoLang #CloudHosting #Startup #JuaCloud #DevOps #SideProjects #Jua

---

### Tweet/X

Jua Cloud pricing: pay for what you use. Scale to zero when idle.

Side project gets occasional traffic? Could cost $0/month.

Production app with real traffic? Managed Postgres + Redis + MinIO + SSL + auto-scaling — all included.

juaframework.dev/cloud #JuaCloud #GoLang

---

## Day 24

### LinkedIn Post

"What about the Go binary and the Next.js app — how does Jua Cloud serve both?"

Great question. This is one of the deployment details that trips people up when they try to self-host a Jua app.

You have two things to run:
1. A compiled Go binary (your API server)
2. A Next.js application (your frontend)

Jua Cloud handles this as a unified deployment. When you run `jua deploy`, the CLI:

1. Builds your Go binary with `CGO_ENABLED=0 GOOS=linux go build`
2. Runs `next build` for your frontend
3. Packages both into a container
4. Deploys the container with a reverse proxy that routes `/api/*` to your Go server and everything else to Next.js

Your entire application — backend + frontend — lives under one domain, one deployment, one `jua deploy` command.

No Nginx config. No Docker Compose files. No CORS headaches between different domains.

It's one application. Jua Cloud treats it as one application.

juaframework.dev/cloud

#GoLang #NextJS #Deployment #DevOps #JuaCloud #Docker #FullStack

---

### Tweet/X

Jua app = Go binary + Next.js frontend.

Jua Cloud deploys both as one unit:
- `/api/*` → Go (Gin)
- Everything else → Next.js

One domain. One `jua deploy`. No Nginx config. No CORS issues.

#JuaCloud #GoLang #NextJS #DevOps

---

## Day 25

### LinkedIn Post

Zero-downtime deployments in Jua Cloud — because downtime is unacceptable.

When you run `jua deploy`, here's what happens behind the scenes:

1. Your new container is built and pushed
2. The new version starts and health checks are run (`GET /health`)
3. Once the new instance passes health checks, traffic is shifted to it
4. The old instance is drained gracefully (in-flight requests complete)
5. The old container is terminated

Your users never see downtime. Background jobs that are in-flight continue to completion before the old worker exits. Asynq handles this gracefully — workers finish their current job and then shut down cleanly.

Database migrations run as part of the deployment pipeline with `jua db migrate`, before the new version starts serving traffic.

This is production-grade deployment behavior by default. You don't configure it. You don't think about it.

`jua deploy` and sleep well.

juaframework.dev/cloud

#GoLang #ZeroDowntime #Deployment #JuaCloud #DevOps #Production #Jua

---

### Tweet/X

`jua deploy` does zero-downtime deployment automatically:

1. Build new container
2. Health check new instance
3. Shift traffic
4. Drain old instance
5. Run migrations

You don't configure any of this. It just works. #JuaCloud #GoLang #DevOps #ZeroDowntime

---

## Day 26

### LinkedIn Post

To every Go developer who's ever looked at a Laravel or Rails project and felt a twinge of framework envy — this is for you.

I know what that feeling is. You love Go. You love the performance, the simplicity, the type system, the tooling. But you've watched PHP and Ruby developers spin up full applications in an afternoon while you're still writing middleware and wiring up your router.

"There should be a Go equivalent of this," you've thought. "Why doesn't Go have this?"

You're not wrong. The Go ecosystem has been missing a full-stack, opinionated framework with real conventions and a great developer experience.

Jua is my answer to that.

And I want to build it with the community, not just for the community.

If Jua excites you, here's what I need:

- **Star the repo** — it signals to the community that this matters
- **Try it on a real project** — even a small side project reveals things no amount of internal testing does
- **Open issues** — bugs, confusing behavior, missing features — all feedback is valuable
- **Spread the word** — share this post, tell a Go developer friend

GitHub: github.com/katuramuh/jua

Let's build the Go framework the ecosystem deserves. Together.

#GoLang #OpenSource #Community #Jua #WebDev #FullStack #DeveloperCommunity

---

### Tweet/X

Hey Go devs — you're allowed to have Laravel envy. We've all felt it.

Jua exists because the Go ecosystem deserves a full-stack framework with real conventions.

Star the repo. Try it. Tell me what's broken: github.com/katuramuh/jua #GoLang #OpenSource #Jua

---

## Day 27

### LinkedIn Post

Real talk from a beta tester:

"I've been building Go APIs for 4 years. Every project started the same way — 2 days of boilerplate before I could write a single line of business logic. Authentication, routing structure, middleware, database connection, error handling patterns... I just copy-paste from the last project and hope I got it right.

With Jua, I ran `jua new` and had a production-ready structure in under 5 minutes. The generated code is clean enough that I'd have written it the same way myself — it's not opinionated in a way that fights you.

The admin panel alone saved me a week of work on my last project. Usually I build a rough admin interface with React and a table library. Jua just... had it. With proper pagination, search, and forms.

I'm not going back."

This is exactly why I built Jua. The boilerplate isn't hard to write — it's just tedious. And tedious things are what tools are for.

If this resonates with you, give Jua a try.

juaframework.dev

#GoLang #DeveloperExperience #OpenSource #Jua #WebDev #FullStack #BackendDevelopment

---

### Tweet/X

"The admin panel alone saved me a week of work. Usually I build a rough admin UI myself. Jua just had it — pagination, search, forms, all generated."

This is what Jua is for. Stop rebuilding the same things. #GoLang #Jua #DeveloperExperience

---

## Day 28

### LinkedIn Post

The Jua roadmap — here's what's coming.

We launched. People are using it. The community is growing. Here's what I'm building next:

**Near-term (next 4-6 weeks)**
- WebSocket support with room management
- More field types in the code generator (relations, enums, JSON)
- Seeder generator: `jua generate seed Post`
- Improved admin panel — custom actions, bulk operations
- Jua Cloud dashboard with metrics and logs UI

**Medium-term (2-3 months)**
- Multi-tenant support built into the framework
- `jua generate api` — API-only project without frontend
- Plugin system for third-party integrations
- CLI tool for managing Jua Cloud resources
- GraphQL API generation alongside REST

**Long-term vision**
- Jua as a platform for indie developers — built-in billing (Stripe), auth providers (Google, GitHub), and analytics primitives
- Visual studio companion — a GUI for the CLI
- Jua Marketplace — installable modules (blog, e-commerce, CMS, auth)

The foundation is solid. The framework is production-ready today. The best features are still ahead.

Follow along: github.com/katuramuh/jua

#GoLang #OpenSource #Roadmap #Jua #WebDev #FullStack #DeveloperTools

---

### Tweet/X

Jua roadmap highlights:

→ WebSocket support
→ Seeder generator
→ Multi-tenant built-in
→ GraphQL generation
→ Jua Marketplace (installable modules)
→ Visual studio companion

Foundation is live. The best is coming. github.com/katuramuh/jua #GoLang #Jua #Roadmap

---

## Day 29

### LinkedIn Post

A question I get asked: "Is Jua production-ready?"

Let me answer that honestly.

The core framework — Go API (Gin + GORM), Next.js frontend, admin panel, background jobs, caching, file storage, email, AI integrations — is production-ready. It's built on battle-tested libraries. Gin powers some of the busiest APIs on the internet. GORM is used in production by thousands of companies. Redis, asynq — rock solid.

The generated code is idiomatic Go that you own completely. There's no runtime framework magic happening in production. Your compiled Go binary doesn't even know it came from Jua.

What's still maturing:

- The CLI will gain more options and generators
- The admin panel is functional but will get more customization options
- Jua Cloud is in early access — it works, but the monitoring UI is basic
- Documentation is growing alongside the framework

My honest advice: try Jua for your next side project or a new internal tool. You'll get real-world experience with it, contribute feedback that shapes the roadmap, and ship faster than you would starting from scratch.

For brand-new greenfield production apps? I'd say yes — with the expectation that you're adopting an early-stage framework and should follow the repository.

The best way to know is to build something.

juaframework.dev

#GoLang #OpenSource #WebDev #Jua #Production #FullStack #DeveloperTools

---

### Tweet/X

"Is Jua production-ready?"

Honest answer: the core is solid (Gin, GORM, Redis, asynq are all battle-tested). The generated code is yours — no runtime magic.

CLI and Jua Cloud are maturing. Great for side projects + new greenfield apps now. #GoLang #Jua

---

## Day 30

### LinkedIn Post

Day 30. One month since Jua launched. Here's what happened.

I shipped a full-stack Go framework into the world and waited to see if anyone cared.

Turns out, people care.

Go developers have been waiting for something like this. The messages I've gotten from developers who've tried it — who finally feel like they can build a complete application in Go without spending a week on infrastructure — that's why I built this.

This is just the beginning.

Here's my ask as we close out month one:

**Try Jua today.** `jua new yourproject`. Build something. Even a small thing.

**Star the repo.** github.com/katuramuh/jua — it takes 2 seconds and it matters more than you think for visibility.

**Join the conversation.** Open an issue. Start a discussion. Tell me what you wish Jua could do.

**Deploy with Jua Cloud.** `jua deploy` — see what it feels like to ship a Go app in 60 seconds.

The Go ecosystem is about to have its Laravel moment. I believe that. And I want to build it with developers who believe it too.

Go + React. Built with Jua. Let's go.

juaframework.dev | github.com/katuramuh/jua

#GoLang #OpenSource #React #FullStack #Jua #JuaCloud #WebDev

---

### Tweet/X

Month 1 of Jua complete.

Go developers wanted a full-stack framework with real conventions. We built it.

Now: star the repo, try `jua new yourproject`, deploy with `jua deploy`.

Go's Laravel moment is here. Let's go. github.com/katuramuh/jua #GoLang #Jua #OpenSource

---

Tweet 2:

30 days. One framework. Two products.

`jua new myapp` → build with Go + React
`jua deploy` → ship to Jua Cloud in 60 seconds

This is just day 30. The best is ahead. juaframework.dev #GoLang #Jua #JuaCloud

---

*End of 30-Day Content Plan*
*Creator: katuramuh | juaframework.dev | github.com/katuramuh*
